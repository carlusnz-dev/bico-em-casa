spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bicoemcasa
    username: bicoemcasa
    password: ${DB_PASSWORD:bicoemcasa}

app:
  rsa:
    private-key-path: ${user.home}/.bicoemcasa/keys/private.pem
    public-key-path: ${user.home}/.bicoemcasa/keys/public.pem
