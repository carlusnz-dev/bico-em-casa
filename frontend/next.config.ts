import type { NextConfig } from 'next';

/**
 * Configuração do Next.js para o frontend do Bico em Casa.
 *
 * Mantida enxuta de propósito: a fundação ainda não tem requisito que
 * justifique opções além do padrão (rewrites, headers, imagens externas etc.
 * entram aqui quando existir necessidade real).
 */
const nextConfig: NextConfig = {};

export default nextConfig;
